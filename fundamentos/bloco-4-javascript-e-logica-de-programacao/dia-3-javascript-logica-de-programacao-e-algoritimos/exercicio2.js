let n = 5;
let simbolo = '*';
let line = '';

//No caso abaixo o for vai vai verificar se a linha atende a condição, tamanho do n, atendendo essa condição ele desenha a linha e adiciona um contador até atingir o valor de N.
for(index = 0; index <= n; index += 1){
    console.log(line);
    line = line + simbolo;
}

